import math

"""
from math import pi,sqrt
"""
print(math.sqrt(60))
from datetime import date

print(date.today())


from random import randint, choice

rand_choice = choice(["Apple", "banana", "kartik"]), randint(1, 10)
print(rand_choice)
